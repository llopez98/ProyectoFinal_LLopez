export default class UserRegister {
    constructor(nombre, contrasena, contrasenaConfirm, tipo){
        this.nombre = nombre;
        this.contrasena = contrasena;
        this.contrasenaConfirm = contrasenaConfirm;
        this.tipo = tipo;
    }
}